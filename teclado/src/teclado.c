#include "chip.h"
#include "main.h"

int main(void) {
char aux;

	sticklpc1769();

	if(aux==1 && aux==2 && aux==3 && aux==4 && aux==5 && aux==6 && aux==7 && aux==8 && aux==9)
	{

	}

	while(1) {
        i++ ;
    }
    return 0 ;
}
